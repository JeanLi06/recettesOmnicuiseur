<!--page qui permet d'afficher une recette parmi d'autres-->

<p>Recette N° <?= $_SESSION['indexCurrentRecipe'] + 1 ?> / <?= $recipeQuantity ?></p>
<nav id="recipe_navigation">
    <a href="<?php echo 'index.php?page=recipes&action=first' ?>" title="Première"><i class="fas fa-fast-backward"></i></a>
    <a href="<?php echo 'index.php?page=recipes&action=previous' ?>" title=Précédente"><i
                class="fas fa-step-backward"></i></a>
    <a href="<?php echo 'index.php?page=recipes&action=next' ?>" title="Suivante"><i
                class="fas fa-step-forward"></i></a>
    <a href="<?php echo 'index.php?page=recipes&action=last' ?>" title="Dernière"><i
                class="fas fa-fast-forward"></i></a>
</nav>
<article id="view_recipe_article">
    <h3><?= htmlspecialchars($recette_from_id['name']) ?></h3>
    <div>
    <figure>
        <img src="img/<?= htmlspecialchars($recette_from_id['photo']) ?>"
             alt="<?= htmlspecialchars($recette_from_id['name']) ?>" class="viewRecipeImage">
    </figure>
    <section>
        <h5>Catégorie&nbsp;:&nbsp; <span><?= htmlspecialchars($recette_from_id['category']) ?></span></h5>
        <h5>Pour <span><?= htmlspecialchars($recette_from_id['how_many_persons']) ?></span> personnes</h5>
        <h5>Temps de cuisson : <span><?= htmlspecialchars($recette_from_id['cooking_time_minutes']) ?> mn</span></h5>
    </section>
    </div>
    <h4>Ingrédients :</h4>
    <p><?= nl2br(htmlspecialchars($recette_from_id['ingredients_list'])) ?></p>
    <h4>Instructions :</h4>
    <p><?= nl2br(htmlspecialchars($recette_from_id['cooking_instructions'])) ?></p>
    <h4>Notes :</h4>
    <p><?= nl2br(htmlspecialchars($recette_from_id['note'])) ?></p>
    <h4>Date de création :</h4>
    <p id="recipe"><?= htmlspecialchars($recette_from_id['creation_date_formatted']) ?></p>
</article>
